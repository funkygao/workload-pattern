/**
 * 洗牌摸牌算法：经典的{@code n choose k}问题.
 *
 * <pre>
 * Shuffle sharding is a technique that isolates different tenant's workloads and gives each
 * tenant a single-tenant experience, even if they're running in a shared cluster.
 * This technique minimizes the number of overlapping instances between two tenants.
 * </pre>
 */
package io.github.workload.shufflesharding;